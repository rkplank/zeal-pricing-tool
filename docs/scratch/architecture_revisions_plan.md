# Superseded Architecture Revision Plan

This scratch plan has been superseded by the approved 2026-05-04 v1 scope revision.

Use the canonical documentation instead:

- `docs/decisions_log.md`
- `docs/pricing_algorithm.md`
- `docs/architecture.md`
- `README.md`

The active v1 architecture is now an on-demand read-only review tool with two dashboard screens, append-only `price_recommendations`, CardCash competitor data displayed as reference-only context, and `ebay_weight = 1.0` with no UI.
